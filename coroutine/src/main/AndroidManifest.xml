<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yfbx.coroutine">

    <application>
        <provider
            android:name=".util.AppProvider"
            android:authorities="${applicationId}.app.provider"
            android:exported="false" />
    </application>

</manifest>
